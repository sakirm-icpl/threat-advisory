# VersionIntel Production Configuration Template
# ==============================================
# Copy this file to .env.production and update with your actual values

# Deployment Environment
ENVIRONMENT=production

# Server Configuration
# Replace with your actual production server IP or domain
SERVER_HOST=your-production-server-ip-or-domain

# Database Configuration - PRODUCTION SETTINGS
# Use strong, unique credentials for production
POSTGRES_USER=versionintel_prod
POSTGRES_PASSWORD=your-secure-production-password-min-16-chars
POSTGRES_DB=versionintel_production

# Flask Security Configuration - CRITICAL FOR PRODUCTION
FLASK_ENV=production
FLASK_DEBUG=0

# Generate new secret keys using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-production-secret-key-must-be-32-chars-minimum-change-this
JWT_SECRET_KEY=your-production-jwt-secret-key-must-be-32-chars-minimum

# GitHub OAuth Configuration - PRODUCTION
# 1. Go to https://github.com/settings/developers
# 2. Create a new OAuth App with these settings:
#    - Application name: VersionIntel Production
#    - Homepage URL: https://your-domain.com
#    - Authorization callback URL: https://your-domain.com/auth/github/callback
# 3. Copy the Client ID and Client Secret here:
GITHUB_CLIENT_ID=your-production-github-client-id
GITHUB_CLIENT_SECRET=your-production-github-client-secret

# External API Keys (Optional)
NVD_API_KEY=your-nvd-api-key-if-needed
AI_PROVIDER=gemini
GOOGLE_API_KEY=your-google-api-key-if-needed
GOOGLE_MODEL=gemini-1.5-flash

# Production Domain Configuration
PRODUCTION_DOMAIN=your-production-domain.com
CORS_ORIGINS=https://your-production-domain.com

# Security Configuration
SECURITY_HEADERS=True
HTTPS_ONLY=True

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE_PATH=/var/log/versionintel/app.log